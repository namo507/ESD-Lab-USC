{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    {{- include "esd-lab-dashboard.labels" . | nindent 4 }}
type: Opaque
stringData:
  pagesDeployHookUrl: {{ .Values.secret.pagesDeployHookUrl | quote }}
{{- end }}
