# Python Research Package

`src/` is the Python/R research-analysis package for ingestion, preprocessing,
feature engineering, imputation, modeling, and visualization. It is separate
from the React frontend at `web/src/`.

The name is intentionally retained because tests, scripts, notebooks, and
existing imports use `src.*`. Treat it as the analysis package, not as a second
frontend source tree.
