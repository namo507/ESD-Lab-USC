# HTML data-quality reports generated by generate_data_quality_report.py are saved here.
# Committed HTML reports allow PI review without raw data access.
# PNG/PDF outputs are excluded from git (see .gitignore).
